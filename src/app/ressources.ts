export const baseURL = "localhost:3000"; // Remplacez par votre base URL

export const routes: { [key: string]: boolean } = {
    "/": true,
    "/about": true,
    "/work": true,
    "/blog": true,
    "/gallery": true,
};